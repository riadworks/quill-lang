# Empty base branch

This branch points at the repository's very first commit with all its files removed, so it is a true ancestor of master with an empty tree. A pull request comparing it against master shows the entire codebase as one addition-only diff, for a full-repository CodeRabbit review. Safe to delete once the review is done - never meant to be merged.
